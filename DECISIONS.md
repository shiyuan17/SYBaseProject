# DECISIONS.md

## Purpose

Track durable backend and cross-repo decisions that future agents must preserve.

## Decisions

| ID | Date | Context | Decision | Rationale | Impact | Revisit When |
| --- | --- | --- | --- | --- | --- | --- |
| DEC-20260608-001 | 2026-06-08 | AI memory layer | Maintain repo-local memory files in both backend and frontend repos; update them at task delivery time on demand. | Keeps durable context close to the code while avoiding noisy hook-enforced updates. | Future AI tasks must check and update memory files before final handoff. | If automatic agent dispatch or external memory sync is introduced. |
| DEC-20260608-002 | 2026-06-08 | Backend hooks | Use native backend hook scripts with PowerShell implementations and Git hook wrappers, not a Node hook manager. | Fits Maven/GitLab backend and avoids adding frontend tooling to the Java repo. | Hook maintenance lives under `scripts/hooks`; Windows PowerShell support is first-class. | If the team adopts a shared hook manager across repos. |
| DEC-20260608-003 | 2026-06-08 | Technical specimen registration pathology number editing | Completion accepts an optional `pathologyNo` candidate from the frontend; non-empty candidates must already match the selected application type rule and be unique, allowing the current case's own number. Empty candidates keep backend generation. | Avoids silent backend auto-completion of user-edited candidates while keeping final numbering authority server-side. | Invalid format returns 400 and duplicate occupied number returns 409; frontend previews are candidate-only and not persisted until completion. | If a dedicated pathology number reservation endpoint or different duplicate policy is introduced. |
| DEC-20260608-004 | 2026-06-08 | Slicing workstation print-before-slice flow | Slide printing is a backend-confirmed step via `POST /api/v1/slicings/slide-print`; `completeSlicing` requires printed slides and no longer creates slide numbers or accepts `slideCount`. | Physical slicing must operate on labels already generated/printed for the embedding box, and adjacent merge must happen before printing. | `slicings.task_id` links generated print records to the exact technical task; workbench statistics/lists are split into pending print, pending slice, and completed lists, filtered by `applications.application_type`. | If repeated print/merge, print rollback, or separate slide-label reservation semantics are introduced. |
| DEC-20260608-007 | 2026-06-08 | Tracking and abnormal application list pathology number display | Expose `pathologyNo` directly on `GET /api/v1/applications` list items instead of requiring frontend detail fan-out. | Keeps tracking lists performant and gives the backend one authoritative source for application-to-case pathology number display. | `ApplicationListItemResponse` and related query records include `pathologyNo`; `null` means no pathology case has been generated yet. | If application lists need pathology-number filtering or multiple active cases per application. |
| DEC-20260608-005 | 2026-06-08 | Historical-status lookup for workflow workstations | Preserve default current-stage filtering, but treat explicit business-key queries as historical lookups: `GET /api/v1/technical-tasks/pending?includeAllStatuses=true` may return completed tasks, and `GET /api/v1/specimen-outbounds` with `applicationId`, `specimenNo`, or exact specimen-number-or-barcode `identifier` may return rows outside outbound-ready status. | Frontend users need to retrieve old rows by exact identifiers while ordinary worklists must not suddenly expand to all historical data. Barcode scanners should behave like text input plus Enter while preserving outbound review before submit. | Frontend sibling `../SYBaseProjectWeb` keeps ordinary entry empty for affected pages and gates operations by status; outbound scan/Enter only queues a unique outbound-ready row and does not directly submit transport; backend defaults remain compatible for existing callers that omit the new flags/conditions. | If dedicated history endpoints replace pending/outbound list reuse, if ordinary worklists are redefined to include history, or if outbound scanning should submit transport immediately. |
| DEC-20260608-006 | 2026-06-08 | Flyway sync against local/target DM schemas with repaired historical gaps | `scripts/migration/run-bl-center-flyway.*` defaults `BL_CENTER_FLYWAY_OUT_OF_ORDER=true`, and `BlCenterFlywayCliSupport` also honors `SPRING_FLYWAY_OUT_OF_ORDER`. | Existing DM schemas can contain repaired/deleted history rows while newer migrations are already applied; allowing out-of-order lets idempotent Java repair migrations such as `V75` be backfilled before applying later pending migrations. | Migration sync emits Flyway's reproducibility warning in out-of-order mode; strict validation can be restored by setting `BL_CENTER_FLYWAY_OUT_OF_ORDER=false` before running the script. | If production release policy forbids out-of-order backfill or migration versions are renumbered to remove repaired gaps. |
| DEC-20260609-001 | 2026-06-09 | M2 specimen check-in and transport operator identity | Current request users may submit check-in / transport create / transport outbound / quick outbound without `operatorVerificationToken` only when the request body operator user ID matches the current user; non-current selected operators still require the existing verification token. Specimen confirmation keeps the original verification-token requirement. | The authenticated request already proves the current user's identity, but cross-user operation still needs explicit verification. | DTOs keep `operatorVerificationToken` optional for these transport/check-in endpoints; controllers must not silently trust arbitrary body operator IDs when token is blank. | If product requires re-authentication for every current-user operator action. |
| DEC-20260609-002 | 2026-06-09 | M2 specimen barcode binding semantics | Registering specimens without an explicit barcode stores `specimens.barcode = null`; `POST /api/v1/specimens/{specimenId}/barcode-binding` is the first-write operation for `targetBarcode`. | Barcode binding is an explicit operator workflow, not an automatic registration side effect. Auto-generated `applicationNo-specimenNo` values made every new specimen appear bound before the user scanned/entered a barcode. | `specimens.barcode` is nullable via Flyway `V81`; management summary uses null/blank as `UNBOUND`; label printing for no-barcode registration remains `PENDING` until a barcode is bound and printed. | If barcode preprinting/reservation becomes a separate backend workflow or product wants registration-time generated barcodes again. |
| DEC-20260609-003 | 2026-06-09 | M3 embedding confirmation workflow | `POST /api/v1/embeddings/start` is the confirmation action and persists `EMBEDDING_CONFIRM_PENDING`; `POST /api/v1/embeddings/cancel` may return only that middle state to `PENDING`; `POST /api/v1/embeddings/complete` is the only final-completion action that writes `COMPLETED` and creates downstream slicing tasks. Legacy `IN_PROGRESS` embedding tasks remain completable but are not cancellable through the rollback endpoint. | 包埋确认 and 包埋完成 are distinct business states, slicing must not receive work before final completion, and operators need a correction path before final embedding records exist. | Active embedding queries and timeout policy include `EMBEDDING_CONFIRM_PENDING`; embedding workstation summary pending counts include it, completed records do not; frontend sibling `../SYBaseProjectWeb` must show “包埋确认待完成”, enable “取消包埋” only for this state, and keep completion gated to this state or legacy `IN_PROGRESS`. | If a formal technical-task state machine replaces string status constants, product renames the middle status, or product allows cancelling final completed embedding records. |
| DEC-20260609-004 | 2026-06-09 | M2 specimen workflow identifiers | 标本固定、标本确认、标本入库、标本签收、标本出库推进动作以 `specimenId` 为首选标识，保留 `specimenBarcode` / `specimenNo` 兼容扫码和旧调用；条码绑定只是关联/扫码便利，不是这些流程的前置条件。 | 未绑定条码标本仍可由数据库标本 ID 唯一定位并推进流程；强制条码会把可执行行操作错误拦截。 | Request DTOs keep barcode fields compatible and add `specimenId` / `specimenNo`; lookup priority is `specimenId > specimenBarcode > specimenNo`; duplicate specimen-number matches return 400; receipt normal/direct paths use the same resolver; transport creation prefers `specimenIds`. Sibling frontend sends row-level `specimenId`. | If product redefines barcode binding as a compliance gate before fixation/confirmation/check-in/receipt/outbound. |
| DEC-20260609-005 | 2026-06-09 | M3 slicing pending slide-print merge groups | Pending slide-print merge groups are persisted in backend tables and can only be created from unprinted slicing tasks that form same-patient, same-case/pathology-number, same embedding-box-prefix pairs after natural wax-block sorting. | 合片 must survive refresh and cannot rely on frontend-only grouping; backend validation is the authority that prevents cross-patient/case/prefix pairing. | `GET /api/v1/slicings/workbench` returns virtual merged rows and hides grouped ordinary rows; `POST /api/v1/slicings/slide-print-merge-groups`, `/cancel`, and `/print` are the only supported mutation paths. Sibling frontend consumes `printGroupId`, `mergedPrintGroup`, `taskIds`, `embeddingBoxIds`, and merged `embeddingBoxNo`. | If product allows arbitrary manual grouping, printed-group rollback, or a single physical-slide print result per merge group. |
| DEC-20260610-001 | 2026-06-10 | 系统日志管理与操作审计 | `bl-center` owns the system log query entry for sibling frontend `/system/logs`: 登录日志 reads existing `user_login_logs`; 操作日志 reads `operation_logs`; audit scope is all authenticated permission-protected writes plus explicit `@AuditOperation(sensitiveQuery = true)` GET queries. | Keeps one frontend menu with two tabs while centralizing audit semantics in the backend; ordinary read/list APIs stay out of audit by default to avoid noise. | `SYS_LOG_MANAGEMENT`, `PERM_SYS_LOG_QUERY`, and `PERM_SYS_LOG_DETAIL` are seeded by Flyway; log detail content/failure fields are sanitized and truncated; log-query APIs are themselves audited as sensitive queries without recursive writes. Sibling frontend route visibility uses query permission and lets the backend enforce detail permission. | If export/retention cleanup, ordinary read-query auditing, or another service owning log query is required. |
| DEC-20260611-001 | 2026-06-11 | M5 archive cabinet maintenance | Archive cabinet batch creation and deletion are backend-owned APIs: `POST /api/v1/archive-cabinets/batch` reuses create permission and `DELETE /api/v1/archive-cabinets/{id}` requires `PERM_M5_ARCHIVE_CABINET_DELETE`. Deletion is empty-only and also rejects positions referenced by storage records or material loans. | Cabinet setup/cleanup must be real and permission-protected, while archive history and loan/storage references must not be silently deleted. | `ArchiveWorkflowService` keeps transaction boundaries; repository checks duplicate batch codes, non-empty positions, and historical references. Sibling frontend calls these APIs from `/operation-support/archive` cabinet tree table and must not introduce `SPECIMEN`/标本柜 without a new contract. | If product allows historical reference deletion, changes cabinet type taxonomy, or introduces archive cabinet import/export/scan workflows. |
| DEC-20260611-002 | 2026-06-11 | M5 reagent inventory/template contract | Reagent management is split into templates and stock batches: `reagents` is the template table, `reagent_stocks` is the batch inventory table, and `reagent_stock_events` is the authoritative audit/action trail for inbound, test, consume, start/finish use, status change, and import adjustment events. Sibling frontend `/operation-resources/reagents` renders this as `试剂库存` / `试剂模板` tabs. CSV import/export uses UTF-8 BOM CSV while UI labels the feature as Excel; existing M5 reagent permission codes are reused; operators come from authenticated request context rather than frontend-supplied reagent operator fields. | The legacy desktop flow requires template metadata, batch stock lifecycle, and event history, but adding per-action permission codes or an `.xlsx` dependency would expand scope and risk. Backend-owned operator identity prevents request-body spoofing. | Migration `V86__extend_reagent_inventory_legacy_fields` owns the schema expansion; `/api/v1/reagents`, `/api/v1/reagent-stocks`, stock action/event endpoints, and import/export form the stable cross-repo contract. Frontend request bodies must keep omitting `operatorName` / `operatorUserId` for reagent operations. | If product requires true `.xlsx`, separate per-action permission codes, hard template deletion, or a medical-order source other than `medical_order_items.id`. |
| DEC-20260611-003 | 2026-06-11 | M5 archive object list contract | `GET /api/v1/archive-objects` is the object全集 pagination endpoint for application forms, wax blocks, and slides; `GET /api/v1/archive-records/search` remains a storage-record/history query and must not be used to represent unarchived object lists. | Storage records are absent for unarchived objects, so archive-record search cannot satisfy archive tab “all objects” semantics. | The endpoint reuses `PERM_M5_ARCHIVE_QUERY`, validates `objectType` against `APPLICATION_FORM`, `EMBEDDING_BOX`, and `SLIDE`, queries real business tables, left joins archive/loan state, and returns only list fields needed by sibling frontend `/operation-support/archive`. | If product adds specimen/photo/white-slide archive object types, changes permission separation, or requires exposing additional high-sensitivity report fields. |
| DEC-20260611-004 | 2026-06-11 | M5 specimen archive contract | `SPECIMEN` is now a supported archive object type: `POST /api/v1/archive/specimens` is protected by `PERM_M5_SPECIMEN_ARCHIVE`, and `GET /api/v1/archive-objects?objectType=SPECIMEN` lists specimen archive candidates. `SPECIMEN` is not a material-loan type. | The old-system workflow needs specimen archive, but loan semantics are still only defined for wax blocks and slides. Separate permission keeps backend authorization explicit. | Archive queries validate `SPECIMEN`; storage records may use `object_type='SPECIMEN'`; material-loan APIs must continue to reject or ignore specimens until a separate borrowing contract exists. This extends `DEC-20260611-003`. | If product adds specimen borrowing, specimen-specific cabinet taxonomy, scanner/print/export support, or photo archive linkage. |
| DEC-20260612-001 | 2026-06-12 | M6 dashboard endpoint contract correction | Current backend `bl-center` does not own `POST /api/v1/stat-dashboard/query`; sibling frontend `../SYBaseProjectWeb` `/m6/dashboard` composes existing `POST /api/v1/stat-reports/query` calls for `QUALITY`, `OPERATION`, and `WORKLOAD`. | A user-reported access log showed `/api/v1/stat-dashboard/query` returning 404, and `M6ManagementController` only exposes the report query/export/stat definition endpoints. Reusing report queries keeps the M6 dashboard inside the existing statistics permission surface without a phantom route. | Backend agents should not document or seed `stat-dashboard/query` unless they implement and test a real aggregate endpoint. Frontend records this as `DEC-20260612-002` / `BUG-20260612-003`. | If product requires a backend-owned aggregate dashboard endpoint for performance, caching, or independent authorization. |
| DEC-20260612-002 | 2026-06-12 | Backend governance guardrails parity | Mirror the sibling frontend governance baseline (`../SYBaseProjectWeb` `DEC-20260612-008`): add `scripts/ci/validate-governance.sh` that rejects duplicate `DEC-*` / `BUG-*` / `TD-*` ledger IDs and enforces `PROJECT_STATE.md` required sections plus a 120-line budget, wired into local hooks and a GitLab CI `verify` job. | The backend memory ledgers previously had no machine guardrail (a duplicate `DEC-20260608-004` had already slipped in) and `PROJECT_STATE.md` had grown past the shared 120-line budget; the frontend already proved a lightweight validator catches these. | `scripts/ci/validate-governance.sh` is the single source; `scripts/hooks/pre-commit` invokes it best-effort and `.gitlab/ci/verify.yml` `verify_governance` is the hard gate. Frontend records the symmetric decision as `DEC-20260612-008`. | If the team adopts a shared cross-repo governance runner or moves governance checks into the Maven build. |
| DEC-20260612-003 | 2026-06-12 | Backend governance workflow executability parity (Phase E) | Mirror the sibling frontend governance executability pass (`../SYBaseProjectWeb` `DEC-20260612-010`): the three `AI_CODE_HEALTH_*` sub-files are consolidated into one checklist-style `docs/rules/AI-CODE-HEALTH.md` with Java-flavored rules (CI checklist scripts now cite the consolidated path); `docs/rules/DYNAMIC_WORKFLOW_RULES.md` gains a modifier single-source statement, a Frontend Cross-check modifier name, a trigger quick-reference table, and an explicit cross-repo mirror clause covering the GitHub PR ↔ GitLab MR platform difference; `scripts/ci/validate-governance.sh` adds a 200-line soft budget for `DECISIONS.md` / `KNOWN_BUGS.md` / `TECH_DEBT.md`. | The two repos' workflow rules had drifted: the backend health spec was a 4-file TypeScript-flavored import in a Java repo, workflow selection had no quick-reference, and ledgers had no growth guardrail while the frontend gained all three in its optimization rounds. | Workflow semantics changes in either repo must update the other repo's mirror clause; oversized ledgers archive resolved entries (e.g. under `docs/reports/`) instead of deleting history; health-doc rules stay in the single consolidated file. Frontend records the symmetric decision as `DEC-20260612-010`. | If a shared cross-repo governance doc set replaces per-repo mirrors, or the health checklist needs re-splitting for size. |
| DEC-20260615-001 | 2026-06-15 | M5 material loan status query | `GET /api/v1/material-loans` is the status-aware material-loan query for sibling frontend `../SYBaseProjectWeb` `/operation-support/borrow`, accepting `loanStatus=BORROWED|RETURNED`; `/material-loans/pending` stays as a pending-only compatibility endpoint. | The frontend now has one borrow-record table filtered by status, but dashboard and old pending callers need stable behavior. | The query reuses `PERM_M5_LOAN_QUERY`, defaults omitted status to `BORROWED`, returns `returnedAt/returnedByName` for returned rows, and keeps return rules unchanged. Frontend records the symmetric decision as `DEC-20260615-004`. | If material-loan pagination, new statuses, or separate returned-history authorization/audit requirements are introduced. |
| DEC-20260615-002 | 2026-06-15 | M5 material loan abnormal registration | `POST /api/v1/material-loans/abnormal-records` persists wax-block/slide abnormal-loan records to `material_loan_abnormal_records` and requires `PERM_M5_LOAN_ABNORMAL_REGISTER`. | The sibling frontend borrow page needs durable异常登记, while registered operator identity must remain backend-authenticated and not body-supplied. | Migration `V88__create_material_loan_abnormal_records.sql` owns the schema; `CreateMaterialLoanAbnormalRecordRequest` accepts common abnormal fields plus slide-specific optional fields; backend validates required reason and permissions. Frontend records the symmetric decision as `DEC-20260615-005`. | If abnormal registration gets approval/review states, specimen/white-slide loan types, or separate abnormal permissions by material type. |
| DEC-20260615-003 | 2026-06-15 | M5 physical archive batch contract | `bl-center` owns atomic batch archive endpoints for physical archive rows: `POST /api/v1/archive/embedding-boxes/batch`, `/slides/batch`, and `/specimens/batch`, using `archiveCabinetId + objectIds[]`. | Users select rows and a cabinet; the backend must be the authority for available-position order, validation, rollback, and specimen reminder persistence. | Batch archive deduplicates selected object IDs, rejects disabled cabinets, insufficient available positions, invalid/borrowed objects, and rolls back the whole transaction on failure. Migration `V89__extend_archive_batch_and_specimen_reminder.java` adds nullable `specimen_storage_records.archive_expires_at` and `archive_reminder_days`; `GET /api/v1/archive-objects` returns object status/person/reminder fields for sibling frontend `DEC-20260615-006`. | If explicit per-row position assignment returns, archive cabinets gain a new hierarchy allocation policy, or specimen reminder fields move to a dedicated table. |
| DEC-20260615-004 | 2026-06-15 | M5 archive cabinet node contract | `bl-center` owns `archive_cabinet_nodes` and `GET/POST/PATCH /api/v1/archive-cabinet-nodes` for AREA / CABINET / DRAWER hierarchy consumed by sibling frontend `../SYBaseProjectWeb` `/operation-support/archive`. | The desktop-compatible cabinet popup needs parent node, node type, code, cabinet type, capacity, path location, and remarks as a durable contract, while remaining capacity must be derived from `archive_positions` instead of user input. | Migration `V90__create_archive_cabinet_nodes.sql` backfills existing cabinets into area/cabinet/drawer nodes. New cabinet nodes create a cabinet, one drawer, and positions; drawer nodes add one layer matching the cabinet slot count; old `/archive-cabinets` and `/archive-cabinets/batch` remain compatible and sync nodes, with batch `parentId` optional. Sibling frontend records `DEC-20260615-007`. | If capacity derivation changes, hierarchy depth expands, legacy `STANDARD` is removed, or test-tube archive actions become real business workflows. |
| DEC-20260615-005 | 2026-06-15 | M4 pathology formal-report distribution contract | `bl-center` exposes current-case formal report versions through `GET /api/v1/pathology-cases/{id}/formal-report-versions` and keeps report distribution state separate from lifecycle state. `report_versions` now owns `print_status/printed_at` and `delivery_status/issued_at/recalled_at`, while `pathology_reports.report_status` and existing sign/publish flow stay unchanged. Batch `POST /api/v1/pathology-reports/formal-versions/print|issue|recall` operate on `versionIds[]`, record service-side current time, require formal versions only, require print-before-issue, and return per-version success/skip summaries. | The sibling frontend pathology report page must show multiple signed/published versions and support operational distribution actions without corrupting the original draft/submitted/reviewed/signed/published lifecycle or forcing edits through historical versions. | Migration `V91__extend_report_versions_distribution_fields.sql` extends the durable schema; list queries return only `SIGNED` / `PUBLISHED` versions sorted by newest formal timestamps; issue/recall semantics are idempotent-by-state and surfaced back to the frontend as partial-success summaries. Frontend records the symmetric UI/contract decision as `DEC-20260615-009`. | If product adds manual timestamp selection, separate permissions for distribution actions, editing of historical formal versions, or a different formal-report source than `report_versions`. |
| DEC-20260615-006 | 2026-06-15 | M4 revision/consultation workbench aggregate contract | `bl-center` keeps `GET /api/v1/pathology-cases/{id}/diagnostic-workbench` as the single case-scoped read contract for sibling frontend `../SYBaseProjectWeb` revision and consultation workbench pages. `consultations[]` now includes `participants[]` detail (`participantId`, `participantUserId`, `participantName`, `participantRole`, `opinion`, `draftedByName`, `commentedAt`) and the same shape is mirrored into tracking-style payloads that reuse the workbench summary records. | The frontend workbench redesign needs row-local consultation opinion entry without adding new list endpoints or forcing users to type participant IDs. Extending the existing aggregate avoids API surface sprawl and keeps participant visibility tied to the case-scoped diagnosis/tracking contract. | Backend query/service/view/controller layers must preserve the `participants` collection on workbench and mirrored tracking responses; frontend records the symmetric UI/query decision as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260615-010`. | If product later requires server-side all-case pagination, independent consultation/revision list endpoints, or a different participant identity/audit model. |
| DEC-20260616-001 | 2026-06-16 | M5 仪器设备管理旧系统对齐契约 | `bl-center` 扩展 `equipment_records` 旧系统档案字段，并以 `GET/POST/PATCH /api/v1/equipment-records` + `POST /api/v1/equipment-records/batch-status` 作为 sibling frontend `../SYBaseProjectWeb` `/operation-resources/equipment` 的唯一设备宽表契约；批量状态只接受 `ACTIVE` / `DISABLED`，复用现有 `PERM_M5_EQUIPMENT_UPDATE`。 | 旧系统设备档案字段、恢复/禁用与宽表查询都需要稳定的数据库/接口契约，且导出/打印已明确留在前端，不应再拆出新的设备专用后端接口。 | Migration `V92__extend_equipment_records_legacy_fields.sql` owns the schema expansion; `equipmentCode` remains the durable unique business code while frontend only changes display label to “资产编号”; keyword query must cover 资产编号/归口编号/设备名称 and remain compatible with 型号/厂家/RFID; `nextMaintenanceAt` stays dedicated to保养记录与预警. Frontend records the symmetric decision as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260616-001`. | If product adds dedicated backend export/print endpoints, changes batch-status permissions, or redefines which legacy fields belong to the main equipment dialog. |
| DEC-20260616-002 | 2026-06-16 | M5 白片借记独立库存/借记合同 | `bl-center` 为 sibling frontend `../SYBaseProjectWeb` `/operation-support/borrow` 新增独立白片借记链路：`white_slide_stocks` 管库存，`white_slide_loans` 管借出/归还记录；API 为 `GET /api/v1/white-slide-stocks`、`GET/POST /api/v1/white-slide-loans`、`POST /api/v1/white-slide-loans/{id}/return`；权限独立为 `PERM_M5_WHITE_SLIDE_QUERY/CREATE/RETURN`。白片不进入现有 `material_loans` 的 `EMBEDDING_BOX` / `SLIDE` 分支。 | 白片截图字段、库存模型、打印语义和权限边界都不同于既有玻片借记；若继续伪装成 `SLIDE`，会混淆库存扣减、查询字段、打印模板和授权范围。 | Migration `V95__create_white_slide_loan_tables.sql` owns the schema and seeds a default stock row for immediate usability; service/repository/controller layers enforce active stock,库存不足校验、借出扣减、归还回补和仅 `BORROWED` 可归还。 Frontend records the symmetric decision as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260616-002`. | If product later merges white-slide borrowing into a unified material-loan center, adds a dedicated white-slide inventory-management UI, or requires server-side print/audit/reader-device integration. |
| DEC-20260616-003 | 2026-06-16 | M5 医疗废物管理最小后端契约 | `bl-center` 为 sibling frontend `../SYBaseProjectWeb` `/operation-resources/medical-waste` 新增最小可用 `medical-waste` 模块：数据库表为 `medical_waste_specimen_batch`、`medical_waste_specimen_batch_label`、`medical_waste_reagent_bag`；接口为 `GET /api/v1/medical-waste/specimen-batches`、`GET /specimen-options`、`POST /specimen-batches/preview-labels`、`POST /specimen-batches/print`、`POST /specimen-batches/{id}/destroy`、`GET/POST /reagent-bags`、`POST /reagent-bags/{id}/handover`。控制器授权复用现有 M5 资源页权限集合，不新增独立医疗废物权限码。 | 前端需要从 placeholder 切到真实医疗废物工作台，但当前前后端都没有现成废物契约；最小闭环必须明确表结构、接口边界、权限复用和标签来源，避免后续重复造一套独立权限或标签数据。 | 人体标本标签预览/打印只能基于现有 grossing/label 工作流数据生成；后端必须拦截空明细打印、重复销毁、重复交接，并允许药物试剂本轮仅做保存打印 + 单次交接，不扩展更复杂销毁/签名状态机。 Frontend records the symmetric decision as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260616-003`. | If product later requires dedicated medical-waste permissions, server-side print gateway integration, complex handover signatures, or a standalone medical-waste label master independent from grossing/label records. |
| DEC-20260616-004 | 2026-06-16 | M6 工作量报表周期趋势合同 | `/api/v1/stat-reports/query` and `/export` now accept optional `periodMode=month|quarter|year` for the sibling frontend M6 workbench; `OPERATION` / `WORKLOAD` rows return `trendPoints` for the workload tab’s month/quarter/year comparison charts, while existing report rows keep their current `metricStatus`, `numerator`, `denominator`, and `sourceNote` shape. | Goal 3 needs real workload period comparison without inventing a new dashboard endpoint; the report contract already owns the stable cross-repo statistics surface. | `StatisticsService.queryReport` builds workload trend buckets from the requested time range and period mode, and frontend `../SYBaseProjectWeb` uses `trendPoints` as the preferred chart input while still falling back to row-based comparison when the backend returns no trend series. Frontend records the symmetric decision as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260616-005`. | If product introduces a dedicated workload aggregate endpoint, different period taxonomy, or separate permission / pagination semantics for workload charts. |
| DEC-20260616-005 | 2026-06-16 | M6 质量指标趋势、分布与明细合同 | `/api/v1/stat-reports/query` quality rows may return `trendPoints` and `breakdowns`; `POST /api/v1/stat-reports/details/query` and `/details/export` are the only backend-owned quality detail drilldown/export paths for the sibling frontend M6 workbench. | Goal 4 needs usable 病理十三项质控 analysis with trend, reason/status distribution, source notes, and paged detail evidence while keeping the existing report endpoint surface stable. | `StatisticsService` derives quality trend buckets and breakdowns from existing quality detail datasets; detail responses expose non-sensitive case identifiers, status, reason, counts, paging, and availability metadata. Do not add `/api/v1/stat-report-details/*` or a temporary dashboard/detail endpoint. Frontend records the symmetric decision as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260616-006`. | If product adds richer indicator-specific detail schemas, critical-value reason data capture, or a dedicated report-workbench aggregate endpoint. |
| DEC-20260616-006 | 2026-06-16 | M6 critical-value read-only statistics contract | `bl-center` adds `QC_CRITICAL_VALUE_COUNT`, `QC_CRITICAL_VALUE_REPORT_TIMELINESS_RATE`, and `QC_CRITICAL_VALUE_REASON_ANALYSIS_COUNT` as `QUALITY` stat indicators through `V97__seed_m6_critical_value_stat_indicators.sql`; all three reuse `POST /api/v1/stat-reports/query`, `/details/query`, and `/details/export`, using `user_notifications.topic_code='CRITICAL_VALUE'` as a read-only proxy source. | Goal 5 needs critical-value count, report timeliness, reason distribution, details, and export before the cross-module notification confirmation / technician reason-entry workflow exists. A read-only notification-center proxy keeps the report contract useful without inventing process data. | Count and reason-analysis rows are `PARTIAL`; timeliness treats `read_at` within 30 minutes of `created_at` as pass. No new menu or permission seed is added; existing `PERM_M6_STAT_REPORT_QUERY/EXPORT` protect detail query/export. Frontend records the symmetric decision as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260616-008`. | If real critical-value reason capture lands, if the timeliness SLA changes, or if product requires dedicated critical-value tables/permissions. |
| DEC-20260616-007 | 2026-06-16 | M6 Goal 6-8 statistics indicators | `bl-center` seeds Goal 6-8 quality indicators through `V98__seed_m6_goal6_8_stat_indicators.sql` and serves them through the existing `stat-reports/query/export/details/query/details/export` paths: frozen diagnosis timeliness and grossing/slicing/diagnosis timeout counts, report-change count/doctor/reason distributions, and unqualified-specimen rate/reason distribution. | The report workbench needs real read-only query/detail/export data for Goal 6-8 without adding temporary endpoints, menu seeds, or cross-module workflow side effects. | Frozen analysis uses `diagnostic_tasks`, `samplings`, and `slicings` with default 30-minute SLA source notes; report changes merge `report_versions` version changes and `report_revision_requests`; unqualified specimen analysis uses `specimens.qualified_flag`, `specimens.unqualified_reason`, and fixation-status proxy fallback. Frontend records the symmetric decision as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260616-009`. | If product changes frozen SLA thresholds, adds real report-change reason fields, introduces dedicated specimen quality tables, or asks to include Goal 9 workflow mutations in this statistics surface. |
| DEC-20260616-008 | 2026-06-16 | Built-in workflow role master data naming | Built-in M2/M3/M4 workflow roles keep their existing `role_id` / `role_code` values but their durable `roles.role_name` values are canonical Chinese岗位名 without stage prefixes, e.g. `标本登记员`, `取材员`, `诊断医生`, `医嘱执行员`. Existing databases are repaired by Flyway `V100__repair_builtin_workflow_role_names`; legacy/new seed and reconciliation migrations must write the same names. | Sibling frontend `../SYBaseProjectWeb` system-user role assignment dialog, role lists, and any other role-name displays read `/api/v1/roles` directly from backend master data. Keeping stage prefixes in the DB would force per-page display cleanup and drift across repos. | Future backend changes must not reintroduce `M2/M3/M4` prefixes into built-in workflow `role_name`; only `role_name` changes here, never `role_id`, `role_code`, permission bindings, or user-role relations. Sibling frontend records the symmetric display dependency as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260616-010`. | If product later requires explicit stage prefixes in backend-owned role master data again, or introduces a separate display-name/i18n layer for roles. |
| DEC-20260617-001 | 2026-06-17 | M2 tracking application list pathology-number query | `GET /api/v1/applications` now supports optional fuzzy `pathologyNo` filtering for the M2 “追踪与异常” application-list tab, while keeping `items[].pathologyNo` as the list-column source and leaving the specimen tab contract unchanged. | Users need to locate an application directly by pathology number from the tracking page; extending the existing application list endpoint keeps the lookup on the same list workflow without adding a separate tracking-search API. | Frontend `TrackingApplicationListView` may submit `pathologyNo` together with application number, patient name, and date range; backend application list controller/query/repository must preserve fuzzy matching semantics and unchanged response shape. | If product later requires exact/normalized matching, expands pathology-number query to the specimen tab, or introduces a dedicated tracking search endpoint. |
| DEC-20260617-002 | 2026-06-17 | M2/M5 患者身份字段与归档标本列表合同 | `bl-center` 归档记录、借阅记录和标本管理列表中的患者身份相关字段必须保持稳定语义：页面标签“病人ID”对应 `patientId`，不能再让 sibling frontend `../SYBaseProjectWeb` 用 `caseId` 代替；标本下游与归档查询链路应尽量直接返回 `patientId`，并在已有稳定来源时一并返回 `inpatientNo`、`patientGender`、`wardName`。 | 前端下游页面与归档借阅列表出现了“病人ID 显示 CASE-...”“住院号/病区/性别缺失但申请单已有真实值”的问题，根因是后端合同没有把患者身份语义和病例 ID 明确分开，导致前端只能猜字段或额外补查。 | Archive query models now expose `patientId` and available patient base fields; specimen management list models expose `inpatientNo` and `wardName` to reduce downstream补数压力. Future backend query/DTO/assembler changes must preserve these field names and semantics so sibling frontend `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260617-003` can bind them directly without fallback to `caseId`. | If product later introduces a different patient-identity display field, removes ward display from downstream pages, or splits archive/specimen patient metadata into dedicated nested objects. |
| DEC-20260617-003 | 2026-06-17 | M3 technical workflow workstation work-date filtering | `bl-center` keeps the M3 work-date contract as single-day `workDate=YYYY-MM-DD`: `/api/v1/medical-orders/pending` filters `medical_orders.order_date`, `/api/v1/slicings/workbench` uses the selected date for stats/pending print/pending slice/completed lists, and `/api/v1/pathology-cases/{caseIdentifier}/technical-tracking` first resolves the case then filters detail collections by their own timestamp fields. | The sibling frontend needs real date filtering for technical workflow workstations, not display-only controls. Technical tracking must keep the case header while returning only date-matching details and the structure nodes referenced by those details. | Superseded by `DEC-20260617-005`. This row is retained only as the earlier same-day contract baseline mirrored from `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260617-004`. | If rollback analysis needs the original single-date rationale. |
| DEC-20260617-004 | 2026-06-17 | M2 specimen first barcode binding workflow gate | `POST /api/v1/specimens/{specimenId}/barcode-binding` treats first-time barcode binding as an independent operator action and no longer blocks unbound specimens just because they are already `CHECKED_IN`, `RECEIVED`, `REJECTED`, or `RETURNED`; `PUT` rebind and `DELETE` unbind keep the existing workflow lock. | Product clarified that barcode association is not a prerequisite gate for the downstream specimen workflow and may happen after the physical flow has already advanced, but relaxing rebind/unbind would let operators rewrite historical barcodes in late stages. | Backend `SpecimenBarcodeBindingService` must bypass workflow-status rejection only for initial `POST` binding while still enforcing specimen existence, blank/unbound precondition, target-barcode validity, and barcode uniqueness. Sibling frontend records the symmetric behavior contract as `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260617-005`. | If product later also wants late-stage unbind/rebind, introduces a dedicated barcode history/audit workflow, or restores barcode binding as a mandatory upstream compliance gate. |
| DEC-20260617-005 | 2026-06-17 | M3 technical workflow visible-page date-range contract | `bl-center` visible technical-workflow menu pages now prefer optional `dateFrom` / `dateTo` (`YYYY-MM-DD`) over legacy single-day `workDate`: `/api/v1/pathology-cases/{caseIdentifier}/technical-tracking`, `/api/v1/embeddings/workstation-summary`, `/api/v1/slicings/workbench`, and `/api/v1/medical-orders/pending` all accept range bounds, use `dateFrom/dateTo` when present, and keep `workDate` only as backward-compatible fallback. Range semantics are end-user inclusive dates implemented internally as end-exclusive timestamps where needed. | The sibling frontend now standardizes visible M3 pages on `SpecimenReceiptView`-style `daterange` filters with empty-default state and no implicit today filter, so the backend contract also had to stop forcing single-day `workDate` semantics. | Query/service/repository layers must centralize range translation so tracking, embedding summary, slicing workbench, and medical-order pending use consistent precedence and timestamp-field handling. This supersedes `DEC-20260617-003` and mirrors frontend `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260617-006`. Targeted Maven tests passed; authenticated browser verification remained blocked by the frontend login slider captcha. | If product restores single-day-only filters, changes inclusive/exclusive date semantics, or extends the same range contract to currently hidden M3 routes. |
| DEC-20260617-006 | 2026-06-17 | M2 申请登记工作台标本字典科室过滤合同 | `bl-center` 新增 `GET /api/v1/application-registration-workbench/specimen-dictionary` 作为申请登记工作台标本字典唯一读模型：后端基于当前登录用户 `departmentId` 过滤 backend-controlled 静态字典 `groups / entryOptions / commonOptions`，若当前用户无科室或该科室无任何关联词条，则回退返回全量并标记 `departmentFiltered=false`。不扩展 `GET /api/v1/auth/me`。 | 前端原先只有本地静态字典与“常用项切半”伪科室逻辑，无法按真实登录科室过滤，也无法保证搜索、常用项和字典树对同一份词条集合一致。让后端按当前登录态解析科室能把过滤语义收敛到一处，并避免扩大用户主档和前端全局状态影响面。 | `ApplicationRegistrationWorkbenchAppService` 现在持有受控标本字典主数据及其 `departmentIds` 关联；controller 暴露只读 VO，前端 sibling `../SYBaseProjectWeb/docs/memory/DECISIONS.md#DEC-20260617-007` 必须统一消费该接口，不得再次在浏览器端自行按科室派生或通过另一块常用区绕出其他科室词条。 | If product later introduces a specimen-dictionary maintenance backend, changes “无匹配则回退全量” semantics, or requires exposing department context through `auth/me`. |

## Update Rules

- Add decisions only when they change future behavior or prevent repeated debate.
- Include options/rationale in the `Context` and `Rationale` columns when concise.
- Do not delete decisions; mark superseded in `Impact` or add a new decision.
