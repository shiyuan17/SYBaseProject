# KNOWN_BUGS.md

## Purpose

Track known backend bugs and reproducible failures that future agents should not rediscover from scratch.

## Entries

| ID | Status | Reproduction | Impact | Workaround | Verification |
| --- | --- | --- | --- | --- | --- |
| BUG-20260608-001 | Resolved | Run `.\scripts\hooks\pre-push.ps1`; the prior failure path was `RepositoryFileHealthGateTest` during fast verification. | The former local backend `pre-push` blocker is no longer reproducing in the current baseline. | None required. | `./mvnw.cmd --% -B -ntp clean test -Dsurefire.excludedGroups=slow` passed on 2026-06-08. |
| BUG-20260608-002 | Open | Run `.\mvnw.cmd -pl bl-center -Dtest=TechnicalSpecimenRegistrationIntegrationTest test`. | The technical specimen registration integration suite fails in `shouldCompleteRegistrationIdempotentlyAndCreateSingleGrossingTask` because expected patient id `P-001` differs from an existing UUID-like value. | Use `.\mvnw.cmd -pl bl-center -DskipTests compile` and targeted method filters for compile/feature validation until the fixture/assertion is reconciled. | Reproduced again on 2026-06-08 while delivering pathology number candidate editing; targeted pathology-number registration methods passed. |
| BUG-20260608-003 | Open | Run `.\mvnw.cmd -pl bl-center -Dtest=NumberingServiceTest test` in the current dirty worktree. | Backend compilation fails before tests because unrelated slicing workflow changes have mismatched constructor/method signatures: `TechnicalWorkflowRecords.SlicingWorkbenchQuery`, `TechnicalWorkflowProcessingRecords.CreateSlicingCommand`, and `TechnicalWorkflowRepository.completeSlicingRecord`. | Reconcile the dirty slicing workflow signature changes before relying on full backend test execution. | Reproduced on 2026-06-08 while attempting numbering-related validation for technical registration pathology number candidate editing. |
| BUG-20260608-004 | Open | Run `.\mvnw.cmd "-Dmaven.repo.local=.m2/repository" -pl bl-center "-Dtest=ApplicationQueryAndDetailIntegrationTest" test` in the current dirty worktree. | Spring/Flyway test startup fails before executing the application-list pathology number test because migration version `V78__add_slicing_task_link.sql` is discovered twice. | Resolve the duplicate V78 migration in the dirty worktree, then rerun the application query integration test. | Reproduced on 2026-06-08 while delivering application-list `pathologyNo`; backend main compile still passed. |
| BUG-20260608-005 | Resolved | Frontend M2/M3 pages queried old records by specimen/application/pathology/task identifiers, but backend defaults only returned current-stage/active subsets: `/api/v1/specimen-outbounds` excluded records outside outbound-ready conditions, and `/api/v1/technical-tasks/pending` hid completed technical tasks. | Historical specimens/tasks could not be reviewed from the affected workstations even when the user had exact business identifiers. | Add explicit-condition historical lookup semantics: outbound query returns all matching statuses when `applicationId` or `specimenNo` exists; technical task query supports `includeAllStatuses=true` and `taskId` while default calls remain active-only. | Fixed in `bl-center` query/controller code and covered by `SpecimenWorkflowClosureIntegrationTest#shouldListSpecimenOutboundsWithMixedStatesAndReflectOutboundUpdates` and `TechnicalWorkflowQueryEnhancementIntegrationTest#shouldHideCompletedTechnicalTasksFromPendingListByDefault`; both targeted Maven tests passed on 2026-06-08. |
| BUG-20260608-006 | Resolved | Run `.\scripts\migration\run-bl-center-flyway.cmd sync` against the local DM schema after `V75` had been repaired as `DELETED`, `V77` checksum had been repaired, and `V79/V80` were pending. | Flyway `migrate()` failed validation because the locally resolved Java `V75` was lower than current schema version `78` and out-of-order execution was not configured. | `BlCenterFlywayCliSupport` now reads `BL_CENTER_FLYWAY_OUT_OF_ORDER` / `SPRING_FLYWAY_OUT_OF_ORDER`; migration scripts default `BL_CENTER_FLYWAY_OUT_OF_ORDER=true` for sync. | `.\scripts\migration\run-bl-center-flyway.cmd sync` passed on 2026-06-08, applied `V75` as `OUT_OF_ORDER`, applied `V79` and `V80`, and ended with `current_schema_version=80`, `pending_migration_count=0`, `validation_success=true`. |
| BUG-20260608-007 | Resolved | Frontend `../SYBaseProjectWeb` 标本采集工作台输入不存在的申请单号后，自动创建申请单把展示年龄（如 `35岁` / `35岁0月0天`）传给 `POST /api/v1/applications`；本地 DM 日志显示 `ApplicationPatientIdentityResolver.resolveOrCreate` 插入 `patients.age` 时发生类型转换异常并返回 500。 | 申请单号未命中时无法自动创建并回显申请单；后端患者建档入口对展示型年龄缺少兜底防御。 | Backend `ApplicationPatientIdentityResolver` now stores the leading numeric age, or `null` when no leading digits exist; sibling frontend now sends numeric `patientAge` on its auto-create request and suppresses lookup 404 global toasts. | Fixed in `bl-center/src/main/java/com/company/bl/application/service/ApplicationPatientIdentityResolver.java` and covered by `ApplicationCrudAndWorkflowLockIntegrationTest#shouldNormalizeDisplayAgeWhenAutoCreatingPatient`; targeted Maven test passed on 2026-06-08. Frontend sibling targeted Vitest, `pnpm check:type`, and `pnpm lint` also passed. |
| BUG-20260609-001 | Resolved | Sibling frontend `../SYBaseProjectWeb` 标本入库或标本转运页面已选中当前登录人为操作人，但提交时仍提示“请选择操作人”或因为未提供 `operatorVerificationToken` 被后端拒绝。Backend source check: `SpecimenCheckInRequest`, `CreateTransportOrderRequest`, `OutboundTransportOrderRequest`, and `QuickOutboundTransportOrderRequest` previously required `@NotBlank operatorVerificationToken`. | 当前登录人无法直接作为入库/转运操作人，导致 M2 标本入库和转运出库操作被前后端校验拦截。 | Check-in / transport DTOs now make `operatorVerificationToken` optional; controllers resolve the current request user when token is blank and submitted user ID matches the current user, while non-current selected operators still require `OperatorVerificationService` tokens. | Fixed in `SpecimenControllerAssembler`, `TransportOrderController`, `SpecimenOutboundController`, and related DTOs; covered by `SpecimenWorkflowClosureIntegrationTest`. Sibling frontend targeted Vitest, `pnpm lint`, `pnpm check:type`, targeted `pnpm exec oxfmt --check`, and backend `.\mvnw.cmd -pl bl-center -Dtest=SpecimenWorkflowClosureIntegrationTest test` passed on 2026-06-09. |
| BUG-20260609-002 | Resolved | Sibling frontend `../SYBaseProjectWeb` 条码绑定页未点击“条码绑定”前已显示 `申请单号-标本编号` 形式的标本条码。Backend source check: `SpecimenRegistrationService` previously generated `application.getApplicationNo() + "-" + specimenNo` whenever `RegisterSpecimensRequest.items[].barcode` was blank, and `JdbcSpecimenWorkflowManagementProjectionSupport` treated any non-blank `specimens.barcode` as `BOUND`. | 新登记标本被错误视为已绑定，导致条码绑定页“未绑定 0”、无法通过输入目标条码完成首次绑定；数据库 `specimens.barcode NOT NULL` 也阻止真实未绑定状态。 | Backend now stores `barcode=null` for no-barcode registration items, marks labels `PENDING`, skips empty barcode printing, and relaxes `specimens.barcode` nullability via `V81__relax_specimen_barcode_nullable`; sibling frontend treats null barcode as `UNBOUND` and only displays target barcode after binding. | Fixed in `SpecimenRegistrationService`, `V81__relax_specimen_barcode_nullable`, and `M2CollectionAndLabelIntegrationTest`; `.\mvnw.cmd -pl bl-center -Dtest=M2CollectionAndLabelIntegrationTest test` passed on 2026-06-09. Sibling frontend targeted Vitest (3 files, 22 tests), `pnpm check:type`, and `pnpm lint` passed. |
| BUG-20260609-003 | Resolved | Sibling frontend `../SYBaseProjectWeb` 点击“切片工作站”后请求 `GET /api/v1/slicings/workbench`，本地 DM 日志 traceId `49378b7dfb6348a7b05a3c066b79e1ed` 显示 `JdbcTechnicalWorkflowProcessingQueries.findSlicingWorkbenchRows` 执行 `group by + order by + offset fetch` 查询时抛出 `无效的ORDER BY子句`。 | 切片工作站无法打开，接口返回 500；待打印、待切片、今日完成列表均受同一个分页分组查询影响。 | Query now projects sort expressions as aliases and orders by selected aliases (`task_status_sort`, `timeout_sort`, `expected_completed_sort`, `slicing_created_sort`, `completed_sort`, etc.) so DM does not reject grouped paged ordering. | Fixed in `JdbcTechnicalWorkflowProcessingQueries`; `.\mvnw.cmd -pl bl-center -DskipTests compile` and `.\mvnw.cmd -pl bl-center -Dtest=SlicingWorkbenchIntegrationTest test` passed on 2026-06-09. |
| BUG-20260609-004 | Resolved | Sibling frontend `../SYBaseProjectWeb` 包埋工作站点击顶部“确认包埋”后任务直接进入最终包埋完成并生成切片待办；产品要求该按钮只进入“包埋确认待完成”，右侧“确认包埋完成”才最终完成。Backend source check: embedding start formerly reused generic `IN_PROGRESS` start semantics and completion was the first durable record creation point. | 包埋确认与完成两个业务动作被合并，切片工作站可能过早出现任务，包埋工作站无法保留确认待完成状态。 | Backend added `EMBEDDING_CONFIRM_PENDING`; `/api/v1/embeddings/start` writes this state, `/api/v1/embeddings/complete` accepts it plus legacy `IN_PROGRESS`, then writes final `COMPLETED` and creates slicing tasks. | Fixed in `TechnicalProcessingWorkflowService`, `TechnicalWorkflowSupport`, task repository/query code, timeout policy, and sibling frontend `EmbeddingWorkstationView`; backend `.\mvnw.cmd -pl bl-center "-Dtest=TechnicalWorkflowExecutionIntegrationTest,TechnicalWorkflowQueryEnhancementIntegrationTest" test` and `.\mvnw.cmd -pl bl-center "-Dtest=*Technical*Workflow*,*Embedding*" test` passed on 2026-06-09. |
| BUG-20260609-005 | Resolved | Sibling frontend `../SYBaseProjectWeb` 取材描写工作站点击“取材完成”时，后端日志 traceId `e2d45c5742fe4f2fb722ea7526038c6c` 返回 `409 RESOURCE_CONFLICT` / `Embedding box number already exists`。Business rule confirmed on 2026-06-09: embedding box numbers only need to be unique within the same pathology case, not globally. | 常见短盒号 `A1/B1/C1` 在不同病理号间复用时被全局唯一约束和全局 repository lookup 误拦截，导致取材完成失败。 | Backend now checks embedding box number conflicts by `case_id + embedding_box_no`, Flyway `V82__scope_embedding_box_number_by_case.sql` replaces the global unique constraint with a case-scoped one, and database baseline/reference SQL comments now state “病例内唯一”. Sibling frontend now generates short box numbers and skips numbers already present in current-case tracking. | Fixed in `TechnicalGrossingWorkflowService`, `TechnicalProcessingWorkflowService`, `TechnicalWorkflowRepository`, `JdbcTechnicalWorkflowProcessingQueries`, `V82__scope_embedding_box_number_by_case.sql`, and sibling frontend `useGrossingWorkbench.ts`; backend `.\mvnw.cmd -pl bl-center clean -Dtest=TechnicalWorkflowExecutionIntegrationTest test` passed on 2026-06-09. Sibling frontend targeted Vitest, `pnpm lint`, and `pnpm check:type` passed. |
| BUG-20260609-006 | Resolved | Sibling frontend `../SYBaseProjectWeb` 切片工作站「玻片打印」列表中“蜡块号”“包埋备注”显示 `-`。Backend source check: the workbench query only joined `embedding_boxes` when `technical_pending_tasks.object_type='EMBEDDING_BOX'`; legacy slicing tasks pointing to `SAMPLING_BLOCK` could not project `embeddingBoxNo` / `embeddingRemarks`. | 切片人员无法确认待打印玻片对应蜡块号和包埋备注，尽管正式包埋盒号在 `embedding_boxes.embedding_box_no` 中非空。 | Workbench query now joins legacy `SAMPLING_BLOCK` rows through `sampling_blocks` and `embedding_boxes.sampling_block_id`; `embeddingRemarks` prefers final embedding remarks and falls back to `sampling_blocks.embedding_remarks`. Sibling frontend keeps consuming backend `embeddingBoxNo` instead of substituting internal IDs. | Fixed in `JdbcTechnicalWorkflowProcessingQueries` and covered by `SlicingWorkbenchIntegrationTest#shouldExposeSlicingWorkbenchFieldsForLegacySamplingBlockTasks`; `.\mvnw.cmd -pl bl-center "-Dtest=SlicingWorkbenchIntegrationTest" test` passed on 2026-06-09. Sibling frontend targeted Vitest and `pnpm check:type` passed. |
| BUG-20260609-007 | Resolved | Sibling frontend `../SYBaseProjectWeb` 离体确认页按标本编号快捷确认未绑定条码标本时，请求 `POST /api/v1/specimen-removals/confirm-by-identifier`；backend log traceId `ea204176904b4f469f6e5ec3550deaec` showed `quick_confirm_specimen_removal` resolving the specimen, then calling `confirmSpecimenRemoval` with `specimen.barcode() == null`, which reached `findSpecimenByBarcode(null)` and failed in `Map.of("barcode", null)` with `NullPointerException`. | 未绑定条码标本无法通过标本编号完成离体确认，接口返回 500 而不是完成确认或给出业务错误。 | `quickConfirmSpecimenRemoval` now confirms the already resolved specimen directly, so specimen-number quick confirmation no longer depends on barcode binding. | Fixed in `SpecimenReceiptAndRemovalService`; covered by `SpecimenReceiptAndRemovalServiceTest#quickRemovalConfirmationShouldSupportSpecimenNumberBeforeBarcodeBinding`. `.\mvnw.cmd -pl bl-center -Dtest=SpecimenReceiptAndRemovalServiceTest clean test` passed on 2026-06-09. Sibling frontend `FixationVerifyView` and specimen workflow service targeted Vitest passed. |
| BUG-20260609-008 | Resolved | Sibling frontend `../SYBaseProjectWeb` 点击“切片工作站”后请求 `GET /api/v1/slicings/workbench`，本地 DM 日志 traceId `bf7293b74dfa462d9c987cb4a1f117db` 显示 `JdbcTechnicalWorkflowProcessingQueries.findSlicingWorkbenchRows` 抛出 `不是 GROUP BY 表达式`；SQL select 中的 `case when t.object_type = 'EMBEDDING_BOX' ...` 依赖 `t.object_type`，但 grouped 查询未将该字段加入 `GROUP BY`。 | 切片工作站无法打开，接口返回 500；待打印、待切片、今日完成列表均受同一个 workbench 查询影响。 | Query now includes `t.object_type` in the grouped projection fields while preserving existing sort aliases, pagination, and `EMBEDDING_BOX` / legacy `SAMPLING_BLOCK` row resolution. | Fixed in `JdbcTechnicalWorkflowProcessingQueries`; `.\mvnw.cmd -pl bl-center -DskipTests compile` and `.\mvnw.cmd -pl bl-center "-Dtest=SlicingWorkbenchIntegrationTest" test` passed on 2026-06-09. |
| BUG-20260609-009 | Resolved | Sibling frontend `../SYBaseProjectWeb` 标本出库页扫描/输入条码或标本编号后调用 `GET /api/v1/specimen-outbounds?identifier=<barcode-or-specimenNo>`；backend `SpecimenWorkflowRemovalQuerySupport` passed `identifier` through to repository SQL, which filtered only `s.specimen_no = :identifier or s.barcode = :identifier`, so an application with multiple specimens only returned the scanned specimen. | 出库人员无法在扫码后核对同一申请单下所有标本，可能遗漏同单其他待出库或阻塞标本。 | Backend now resolves a unique `identifier` barcode/specimen-number hit to the owning `applicationId`, clears identifier/specimen filters, and returns the application-scoped outbound list; ambiguous or missing identifier matches keep exact filtering. Sibling frontend records this as `BUG-20260609-019`. | Fixed in `SpecimenWorkflowRemovalQuerySupport` and covered by `SpecimenWorkflowClosureIntegrationTest#shouldListAllApplicationSpecimensWhenFilteringOutboundBySpecimenNoOrIdentifier`; the target Maven suite passed earlier on 2026-06-09, while a later rerun is blocked before specimen assertions by unrelated dirty M3 slicing signature mismatches in `JdbcTechnicalWorkflowProcessingQueries` / `TechnicalWorkflowRecords.SlicingWorkbenchRow`. Sibling frontend targeted Vitest, typecheck, and lint passed. |
| BUG-20260609-010 | Resolved | Sibling frontend `../SYBaseProjectWeb` 标本固定、标本确认、标本入库、标本出库对未绑定条码标本执行行操作时，旧后端请求模型和服务路径只按条码解析，或出库自动创建转运单只构造 `specimen.barcode()` 列表。 | 未绑定条码但已有 `specimenId` 的标本无法推进固定、确认、入库或出库流程，违背“条码绑定可选”的业务规则。 | Backend DTOs and services now resolve workflow requests by `specimenId > specimenBarcode > specimenNo`; duplicate specimen-number hits return 400; transport creation and quick outbound prefer `specimenIds`. | Fixed in `SpecimenWorkflowLookupSupport`, fixation/verification/check-in/transport services, DTOs, and controllers; covered by `SpecimenFixationServiceTest`, `SpecimenVerificationServiceTest`, `SpecimenReceiptAndRemovalServiceTest`, `SpecimenTransportServiceTest`, and `SpecimenWorkflowClosureIntegrationTest`. Targeted Maven suite passed on 2026-06-09 (47 tests); sibling frontend targeted Vitest/lint/typecheck passed. |
| BUG-20260609-011 | Resolved | Sibling frontend `../SYBaseProjectWeb` 标本接收工作站点击“标本签收”时，对未绑定条码标本提交空 `specimenBarcode`；backend `POST /api/v1/specimen-receipts` 因 `ReceiveSpecimensRequest.ReceiptItem.specimenBarcode @NotBlank` 返回 400，前端看到“不能为空”，日志 traceId `0e0f1a97be5f487da5547bdb384eb21a`。 | 条码不是签收前置条件，但未绑定条码标本无法按转运单签收，也无法直接接收。 | `ReceiveSpecimensRequest.ReceiptItem` 移除条码必填并新增 optional `specimenId` / `specimenNo`; receipt normal/direct paths resolve `specimenId > specimenBarcode > specimenNo` and still reject missing or ambiguous identifiers. Sibling frontend receipt payloads now发送三标识并允许 nullable barcode。 | Fixed in `ReceiveSpecimensRequest`, `SpecimenReceiptController`, `SpecimenWorkflowModels`, `SpecimenReceiptAndRemovalService`, and sibling frontend receipt workbench; covered by `SpecimenReceiptAndRemovalServiceTest#directReceiptShouldResolveUnboundSpecimenBySpecimenId` and frontend targeted Vitest. Backend `.\mvnw.cmd -pl bl-center -Dtest=SpecimenReceiptAndRemovalServiceTest test` passed on 2026-06-09. |
| BUG-20260615-001 | Resolved | Sibling frontend `../SYBaseProjectWeb` 归档管理申请单列表/弹窗缺少申请医生、申请时间；后端 `GET /api/v1/archive-objects` / `GET /api/v1/archive-records/search` 的 `ArchiveRecordView` 未返回这些字段。Initial implementation using `applications.applicant_doctor_name` failed H2 integration tests because `applicant_doctor_name` belongs to `specimens`, while `applications` uses `submitting_doctor_name`. | 前端无法展示申请单归档来源医生和申请日期，用户容易误判为源数据为空；错误字段名会导致归档对象查询 500。 | Backend `ArchiveRecordView` now returns `applicantDoctorName` from `applications.submitting_doctor_name as applicant_doctor_name` and `applicationDate` from `applications.application_date`; frontend keeps consuming `applicantDoctorName` / `applicationDate`. Sibling frontend records this as `../SYBaseProjectWeb/docs/memory/KNOWN_BUGS.md#BUG-20260615-003`. | Fixed in `bl-center` archive query models/repository mapper and sibling frontend operation-support archive UI. Backend `cmd /c mvnw.cmd -pl bl-center -Dtest=ArchiveWorkflowIntegrationTest test` passed on 2026-06-15; frontend targeted Vitest, `pnpm check:type`, and targeted format checks passed. |
| BUG-20260615-002 | Resolved | Run `.\scripts\migration\run-bl-center-flyway.cmd sync` with `V89__extend_archive_batch_and_specimen_reminder.sql` containing `ALTER TABLE specimen_storage_records ADD COLUMN IF NOT EXISTS ...` on the DM Flyway connection. Flyway failed while parsing the SQL script before execution: `Unable to parse statement ... Incomplete statement at line 1 col 1`. | `run-bl-center-flywayf` / Flyway sync could not apply the archive reminder columns, blocking local/target schema upgrade beyond version 88. | Replace the SQL migration with Java migration `V89__extend_archive_batch_and_specimen_reminder.java`, which checks table/column metadata and executes plain `ALTER TABLE ... ADD COLUMN ...` only when needed. | Fixed in `bl-center/src/main/java/db/migration/V89__extend_archive_batch_and_specimen_reminder.java`; `cmd /c mvnw.cmd -pl bl-center -DskipTests compile` passed, and `.\scripts\migration\run-bl-center-flyway.cmd sync` passed on 2026-06-15 with `installed=89|extend archive batch and specimen reminder|JDBC|SUCCESS`, `current_schema_version=90`, `pending_migration_count=0`, `validation_success=true`. |
| BUG-20260617-001 | Resolved | Sibling frontend `../SYBaseProjectWeb` 诊断分配页按病理号查询时，申请单列表可显示 `GET /api/v1/applications` 返回的 `pathology_cases.pathology_no`，但 `GET /api/v1/diagnostic-tasks/pending` 仍按 `diagnostic_tasks.pathology_no` 过滤；技术登记人工改病理号后只更新了 `pathology_cases`，未同步 `diagnostic_tasks`，因此会出现“图1有病理号、图2查不到”的漂移。 | 诊断分配按最新病理号无法命中待分配任务，用户误以为任务丢失；历史病例一旦发生病理号修正，诊断任务查询会持续失真。 | `pathology_cases.pathology_no` 继续作为病例病理号主数据；`JdbcSpecimenWorkflowTransportMutationSupport.updatePathologyCasePathologyNo(...)` 现已同步更新同病例全部 `diagnostic_tasks.pathology_no`，并通过 `V101__sync_diagnostic_task_pathology_no_from_cases.sql` 一次性回填历史空值/不一致数据。Sibling frontend records the user-facing symptom as `../SYBaseProjectWeb/docs/memory/KNOWN_BUGS.md#BUG-20260617-001`. | Fixed in `bl-center` persistence + migration + integration tests. Targeted Maven command `cmd /c "mvnw.cmd -pl bl-center -Dtest=DiagnosticWorkflowAssignmentPathologyNoQueryIntegrationTest#shouldListPendingDiagnosticTaskByUpdatedPathologyNoAfterCaseNumberChanges,TechnicalSpecimenRegistrationIntegrationTest#shouldSyncDiagnosticTaskPathologyNoWhenManualCandidateUpdatesCurrentCase,com.company.bl.infrastructure.migration.DiagnosticTaskPathologyNoSyncV101MigrationTest test"` passed on 2026-06-17. Frontend did not require code changes; symptom is resolved through the backend contract/data repair. |
| BUG-20260617-002 | Resolved | Sibling frontend `../SYBaseProjectWeb` 标本接收页输入标本号并回车会调用 `GET /api/v1/specimens?keyword=<specimenNo>&page=1&size=500`，后端标本管理查询引用不存在的 `application_registration_workbench_extensions` 表；同一脏工作区中的归档/借阅查询也引用了该不存在扩展表。 | 标本接收扫码查询返回 500，页面提示服务器内部错误；归档对象/借阅列表也可能在触发相关查询时因同一不存在表返回 500。 | Backend queries now use stable `application_registration_workbench.inpatient_no` / `ward_name` directly and no longer join the nonexistent extension table; `GET /api/v1/specimens` response continues exposing `inpatientNo` / `wardName`. Sibling frontend records the user-facing symptom as `../SYBaseProjectWeb/docs/memory/KNOWN_BUGS.md#BUG-20260617-003`. | Fixed in `JdbcSpecimenWorkflowManagementProjectionSupport`, `JdbcArchiveQueries`, specimen management response mapping, and `ApplicationRegistrationWorkbenchIntegrationTest`. `cmd /c mvnw.cmd -pl bl-center -Dtest=ApplicationRegistrationWorkbenchIntegrationTest#shouldLookupByApplicationNoAndSaveWorkbenchForDownstreamFlow test` passed on 2026-06-17 with `MAVEN_OPTS=-Xmx2g`; the test asserts both `applicationNo` and specimen-number `keyword` list lookups return 200 with `inpatientNo` / `wardName`. |
| BUG-20260617-003 | Resolved | Sibling frontend `../SYBaseProjectWeb` 取材描写工作站对检查号 `BL202606010001` 点击“取材”时，会先调用 `POST /api/v1/grossings/start`，随后刷新 `GET /api/v1/technical-tasks/pending` 并加载 `GET /api/v1/grossings/{taskId}/context`；当前脏工作区引入 `patientIdDisplay` 扩展后，取材任务查询/VO 映射链路在 `application_registration_workbench` 缺失记录时出现字段不一致风险，用户侧表现为“服务器内部错误”。验证回归时还暴露出诊断任务待办汇总 SQL 在 H2 下因 specimen 名称聚合写法触发运行时 500。 | 取材人员无法开始取材，点击按钮后工作站直接报 500；若不锁住该兼容场景，后续待办刷新和上下文加载仍可能被同类空扩展数据击穿。 | Backend keeps the existing `/api/v1/grossings/start` contract and makes `patientIdDisplay` nullable through the technical-task query / row-mapper / response chain, so missing `application_registration_workbench` rows no longer break grossing start or context reload. Validation also simplified the diagnostic pending-task specimen aggregation SQL to avoid a separate runtime 500 during regression execution. Sibling frontend records the symptom as `../SYBaseProjectWeb/docs/memory/KNOWN_BUGS.md#BUG-20260617-006`. | Fixed in sibling backend dirty worktree technical-workflow compatibility chain plus `JdbcDiagnosticTaskStore`; covered by `TechnicalWorkflowQueryEnhancementIntegrationTest#shouldStartGrossingAndLoadContextWithoutRegistrationWorkbenchRecord`. `.\mvnw.cmd -pl bl-center "-Dtest=TechnicalWorkflowQueryEnhancementIntegrationTest,TechnicalWorkflowExecutionIntegrationTest" test` passed on 2026-06-17, and the run logged `POST /api/v1/grossings/start` plus subsequent pending/context requests returning `200` instead of `500`. |
| BUG-20260622-001 | Resolved | Local DM database had `diagnostic_tasks=0` while `technical_pending_tasks` contained completed `STAINING` tasks and cases `BD202606060001` / `BL202606070001` still had `case_status='STAINING'`; sibling frontend diagnosis assignment page called `/api/v1/diagnostic-tasks/assignment` but returned an empty list. | Staining-completed cases never entered M4 diagnosis assignment, so assignment visibility and permission fixes could not show data because the source diagnostic task rows were missing. | Staining completion now locks the pathology case row, completes the current staining task, rechecks whether all staining tasks for the case are complete, then updates the case to `DIAGNOSIS_PENDING`, calls `createPrimaryDiagnosticTaskIfAbsent`, and records `DIAGNOSIS_ASSIGN CREATE SUCCESS`. Flyway `V107__backfill_diagnostic_tasks_after_staining_handoff.sql` backfills historical fully stained `STAINING` cases that have no active primary diagnostic task. | Fixed in `TechnicalProcessingWorkflowService`, technical workflow repository locking, and `V107`. Targeted Maven `rtk ./mvnw.cmd -pl bl-center "-Dtest=TechnicalWorkflowExecutionIntegrationTest#shouldCreateSingleDiagnosticTaskWhenStainingTasksCompleteConcurrently,DiagnosticTaskStainingHandoffRepairV107MigrationTest" test` passed on 2026-06-22. Local DM repair affected 2 diagnostic tasks, 2 workflow events, and 2 case statuses; `GET /api/v1/diagnostic-tasks/assignment?page=1&size=5` returned `total=2` with `BD202606060001` and `BL202606070001`. |
| BUG-20260629-001 | Resolved | Sibling frontend `../SYBaseProjectWeb` 点击“切片工作站”后请求 `GET /api/v1/slicings/workbench`，本地 DM 日志 traceId `d3325352058042f49f3fd2c656071f19` 显示 `JdbcTechnicalWorkflowProcessingQueries.findPrintedSlicingProcessMergeGroupRows` 执行“已打印合并组”查询时抛出 `无效的ORDER BY子句`；该 SQL 在 `group by` 聚合后仍按未投影的 `mg.created_at` 排序。 | 只要切片工作站命中已打印合并组查询，接口就返回 500，待切片列表无法打开。 | Backend now projects `mg.created_at` as `print_group_created_at`, includes it in the grouped fields, and orders by that alias so the DM parser accepts the grouped sort while preserving the original merge-group ordering. | Fixed in `JdbcTechnicalWorkflowProcessingQueries`; `./mvnw -pl bl-center -Dtest=SlicingWorkbenchIntegrationTest test` passed on 2026-06-29, and the existing `SlicingWorkbenchIntegrationTest#shouldExposePrintedMergeGroupAsMergedPendingSliceRow` regression path still returned 200. |

## Update Rules

- Record only concrete, reproducible, or user-reported bugs.
- Keep production issues linked to logs, reproduction steps, and regression tests.
- Move fixed bugs to a resolved status instead of deleting them.
