# Default runtime configuration for the prod-06 wrapper.
# Shared datasource and path settings still come from run-centers.conf.

if [ -n "${RUN_CENTERS_BASE_CONFIG_FILE:-}" ] && [ -f "$RUN_CENTERS_BASE_CONFIG_FILE" ]; then
  # shellcheck disable=SC1090
  . "$RUN_CENTERS_BASE_CONFIG_FILE"
fi

SPRING_PROFILES_ACTIVE=prod-06
