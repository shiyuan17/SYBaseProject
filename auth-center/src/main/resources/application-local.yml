spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:authcenter;MODE=LEGACY;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
  flyway:
    enabled: false
  sql:
    init:
      mode: always

