# M5 验收清单

验收时间：2026-05-21

## 验收结论

- 结论：后端验收通过，待前端联调
- 说明：本次验收按 M5 已承诺范围执行，不包含报表、Excel 导出、采购流程、收费联动、真实文件上传
- 证据：已执行目标回归命令并得到 `BUILD SUCCESS`，共 `16` 个测试通过、`0` 失败、`0` 错误

```powershell
& "$env:USERPROFILE\.m2\wrapper\dists\apache-maven-3.9.9-bin\33b4b2b4\apache-maven-3.9.9\bin\mvn.cmd" `
  -pl bl-center -am test `
  "-Dtest=ArchiveWorkflowIntegrationTest,ArchiveRoleAuthorizationIntegrationTest,OperationSupportIntegrationTest,DiagnosticWorkflowIntegrationTest,MedicalOrderIntegrationTest,DiagnosticRevisionIntegrationTest,M4RoleAuthorizationIntegrationTest,FlywayTableCoverageTest" `
  "-Dsurefire.failIfNoSpecifiedTests=false"
```

## 1. 数据库与权限

- [x] Flyway 迁移可完整执行到 M5，包含 `archive_cabinets`、`archive_positions`、`specimen_storage_records`、`material_loans`、`reagents`、`reagent_stocks`、`equipment_records`、`equipment_maintenance_logs`
- [x] M5 菜单、权限、角色授权已入库，包含 `ROLE_PATHOLOGY_ADMIN`、`ROLE_ARCHIVE_MANAGER`、`ROLE_REAGENT_DEVICE_MANAGER`
- [x] `M5PermissionCodes` 与控制器权限注解一致，无缺码、错码、旧码复用错误
- [x] `FlywayTableCoverageTest` 通过，新增表均被迁移覆盖
- [x] M5 未额外引入 `archives` 表，申请单图片仍复用 `case_media_assets`

验证依据：
- 迁移文件已落地：`V22__create_m5_archive_operation_schema.sql`、`V23__seed_m5_archive_operation_authorization.sql`、`V24__reconcile_m5_archive_operation_authorization.java`
- 本次回归日志显示 Flyway 成功执行到 `v25`，其中包含 `v22`、`v23`、`v24`

## 2. 归档与借阅闭环

- [x] 可创建归档柜，创建时自动生成柜位，`position_code` 符合 `${cabinetCode}-L${layerNo}-S${slotNo}`
- [x] `GET /api/v1/archive-cabinets`、`POST /api/v1/archive-cabinets`、`PATCH /api/v1/archive-cabinets/{id}` 可正常使用
- [x] `GET /api/v1/archive-positions/available` 只返回可用且柜体有效的柜位
- [x] 申请单可归档到柜位，并可写入图片 URL
- [x] 蜡块可归档，并可按 `blockCode/embeddingBoxNo` 检索到归档记录
- [x] 玻片可归档，并可按 `slideNo` 检索到归档记录
- [x] 同一柜位被占用后，重复归档返回 `409`
- [x] 已归档材料可借出，借出后状态变为 `BORROWED`
- [x] 已借出材料重复借出返回 `409`
- [x] 已借出材料归还后状态恢复 `IN_STORAGE`
- [x] 归还默认回原柜位；原柜位不可用时，必须显式指定替代柜位
- [x] 无效归还或重复归还返回 `409`
- [x] `workflow_events` 中可看到 `ARCHIVE / BORROW / RETURN` 事件记录

验证依据：
- `ArchiveWorkflowIntegrationTest` 已覆盖申请单图片归档、蜡块归档、玻片归档、借出、归还、重复占位、重复借出、重复归还
- `ArchiveWorkflowService` 已实现归档事件写入、借出事件写入、归还事件写入

## 3. 医生查询回流

- [x] `diagnostic-workbench` 返回申请单归档字段：`applicationFormArchiveStatus`、`applicationFormArchiveLocation`、`applicationFormImageUrl`
- [x] `diagnostic-workbench.blocks[]` 返回：`archiveStatus`、`archiveLocation`、`loanStatus`
- [x] `diagnostic-workbench.slides[]` 返回：`archiveStatus`、`archiveLocation`、`loanStatus`
- [x] `report-tracking` 返回申请单归档字段：`applicationFormArchiveStatus`、`applicationFormArchiveLocation`、`applicationFormImageUrl`
- [x] 借出后，医生工作台和追踪页能看到对应借出状态
- [x] 归还后，医生工作台和追踪页能看到状态恢复
- [x] M4 既有字段结构未被破坏，老断言保持通过

验证依据：
- `DiagnosticWorkbenchResponse`、`ReportTrackingResponse` 已新增上述回流字段
- `ArchiveWorkflowIntegrationTest` 已验证工作台和追踪页的归档/借阅状态显示
- `DiagnosticWorkflowIntegrationTest`、`DiagnosticRevisionIntegrationTest`、`MedicalOrderIntegrationTest`、`M4RoleAuthorizationIntegrationTest` 本次回归均通过

## 4. 试剂与设备台账

- [x] `GET /api/v1/reagents`、`POST /api/v1/reagents`、`PATCH /api/v1/reagents/{id}` 可正常维护试剂基础信息
- [x] `GET /api/v1/reagent-stocks`、`POST /api/v1/reagent-stocks`、`PATCH /api/v1/reagent-stocks/{id}` 可正常维护批次库存
- [x] 试剂预警接口可返回 `LOW_STOCK` 与 `NEAR_EXPIRY`
- [x] 库存行阈值优先级高于试剂基础阈值
- [x] 试剂库存调整不写 `workflow_events`，而是进入 `operation_logs`
- [x] `GET /api/v1/equipment-records`、`POST /api/v1/equipment-records`、`PATCH /api/v1/equipment-records/{id}` 可正常维护设备台账
- [x] `GET /api/v1/equipment-records/{id}/maintenance-logs`、`POST /api/v1/equipment-records/{id}/maintenance-logs` 可正常维护保养维修记录
- [x] 设备预警接口可返回 `DUE_SOON` 与 `OVERDUE`
- [x] 设备保养记录写入后，可回写 `nextMaintenanceAt`
- [x] 试剂/设备维护已统一接入 `OperationAuditService`

验证依据：
- `OperationSupportIntegrationTest` 已覆盖试剂信息、库存维护、低库存/近效期预警、设备台账、保养记录、到期/逾期预警
- `OperationSupportService` 已统一通过 `OperationAuditService` 包裹新增与维护操作

## 5. 权限与回归

- [x] `USER_M1_ARCHIVE` 可做归档柜、归档、借阅，不能做试剂设备维护
- [x] `USER_M1_REAGENT` 可做试剂设备维护，不能做归档借阅
- [x] `USER_M4_DIAGNOSIS` / `USER_M4_TRACKING` 只能查看回流结果，不能执行归档管理
- [x] 无权限用户访问 M5 受控接口返回 `403`
- [x] `ArchiveWorkflowIntegrationTest` 通过
- [x] `ArchiveRoleAuthorizationIntegrationTest` 通过
- [x] `OperationSupportIntegrationTest` 通过
- [x] `DiagnosticWorkflowIntegrationTest` 通过
- [x] `MedicalOrderIntegrationTest` 通过
- [x] `DiagnosticRevisionIntegrationTest` 通过
- [x] `M4RoleAuthorizationIntegrationTest` 通过
- [x] `FlywayTableCoverageTest` 通过

验证依据：
- `ArchiveRoleAuthorizationIntegrationTest` 已验证 M1 归档角色、M1 试剂角色、M4 只读角色边界
- 本次目标回归结果为 `Tests run: 16, Failures: 0, Errors: 0, Skipped: 0`

## 默认验收结论规则

- [x] 若只有前端未接入、后端接口与测试全部通过：可判定为“后端验收通过，待前端联调”
- [x] 当前不存在权限错配、状态流转错误、回流字段缺失、M4 回归失败中的已知阻塞项
- [x] 因此本次 M5 判定为“后端验收通过，待前端联调”

## 假设

- [x] 本清单只验收 M5 已承诺范围，不含报表、Excel 导出、采购流程、收费联动、真实文件上传
- [x] 本次以后端接口、数据库迁移状态、集成测试结果作为主要验收依据
