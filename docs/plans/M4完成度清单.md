# M4 完成度清单

## 完成结论

- 按 `M4 最小报告闭环实施计划` 判断：首批最小报告闭环已完成。
- 按 `M4 第二批闭环与治理执行计划` 判断：第二批修订审批、技术域医嘱、科内会诊和治理收口已完成。
- 按 `M4 医生诊断与报告闭环规划` 的完整口径判断：当前已完成常规病理主链路下的两批核心闭环，但仍未达到完整 M4 全量范围。
- 本清单只盘点当前仓库内已实现且已验证的内容，不追加新的功能承诺。

## 已完成能力

### 数据库与编号

- 已创建 `diagnostic_tasks`、`pathology_reports`、`report_versions`。
- 已创建 `report_revision_requests`、`medical_orders`、`consultation_cases`、`consultation_participants`。
- 已在 `numbering_rules` 中新增 `REPORT_NO` 规则，并在应用层补齐 `NumberingService.generateReportNo()`。
- 医嘱单号首版按应用内 `nextId("MO")` 生成，不新增独立 `ORDER_NO` 规则。
- M4 病例主状态已覆盖：
  - `DIAGNOSIS_PENDING`
  - `DIAGNOSING`
  - `REPORT_PENDING_REVIEW`
  - `REPORT_REVIEWING`
  - `REPORT_SIGNED`
  - `REPORT_PUBLISHED`
- 由于仓库原有 `V15`、`V16`、`V17` 已被占用，第二批迁移实际落地为：
  - `V18__create_m4_revision_order_consultation_workflow.sql`
  - `V19__seed_m4_batch2_authorization.sql`
  - `V20__reconcile_m4_batch2_authorization.java`

### RBAC 与种子

- 已完成首批 M4 菜单、权限码、角色、测试用户和旧库补齐。
- 已完成第二批权限码补充：
  - `PERM_M4_REVISION_REQUEST_CREATE`
  - `PERM_M4_REVISION_APPROVE`
  - `PERM_M4_MEDICAL_ORDER_CREATE`
  - `PERM_M4_MEDICAL_ORDER_CANCEL`
  - `PERM_M4_MEDICAL_ORDER_QUERY`
  - `PERM_M4_MEDICAL_ORDER_ACCEPT`
  - `PERM_M4_MEDICAL_ORDER_COMPLETE`
  - `PERM_M4_CONSULTATION_CREATE`
  - `PERM_M4_CONSULTATION_COMMENT`
  - `PERM_M4_CONSULTATION_COMPLETE`
- 已新增 `ROLE_M4_MEDICAL_ORDER_EXECUTE` 及测试用户 `USER_M4_ORDER_EXECUTE`。
- 已通过 `V14`、`V20` 兼容旧库和测试库起库场景。

### M3 到 M4 衔接

- 已在 M3 染色完成后的“病例无剩余染色任务”分支接入 M4 投递逻辑。
- 染色全部完成后，病例状态自动推进到 `DIAGNOSIS_PENDING`。
- 同一病例自动投递 1 条病例级 `PRIMARY` 诊断任务。
- “技术链路结束并转入医生诊断”的关键动作已写入 `workflow_events`。

### 医生主链路

- 已完成诊断待办查询、分派、接单、开始诊断。
- 已完成报告草稿创建、暂存、提交、审核通过、审核退回、签发、发布。
- 已完成“签发生成版本快照”和“发布补齐发布版本快照”。
- 已完成发布成功后关闭诊断任务，并推进病例状态到 `REPORT_PUBLISHED`。

### 第二批闭环

- 已完成报告修订申请、审批通过、审批驳回。
- 已完成“当前报告进入新版本草稿”的修订实现，旧版继续保留在 `report_versions`。
- 已完成技术域医嘱创建、待办查询、接单、完成、取消。
- 已完成科内会诊发起、参与人评论、主持人完成。
- 已完成修订、医嘱、会诊在工作台和报告跟踪中的摘要回传。

### 聚合查询与治理

- 已完成 `diagnostic-workbench` 聚合查询。
- 已完成 `report-tracking` 聚合查询。
- 已将聚合查询职责收口到独立 `DiagnosticTrackingQueryRepository`。
- `DiagnosticReportAppService` 继续保留 façade，首批控制器入口保持不变。
- 现有响应结构保持兼容，只追加新字段，不重命名、不删字段。

### 审计追踪

- 诊断分派、接单、开始、草稿创建、提交、审核、退回、签发、发布均写入 `workflow_events`。
- 修订申请、批准、驳回均写入 `workflow_events`。
- 医嘱创建、接收、完成、取消均写入 `workflow_events`。
- 会诊创建、评论、完成均写入 `workflow_events`。
- 报告签发与发布继续写入 `report_versions`，形成版本链。

### 当前已落接口

- `GET /api/v1/diagnostic-tasks/pending`
- `POST /api/v1/diagnostic-tasks/{id}/assign`
- `POST /api/v1/diagnostic-tasks/{id}/accept`
- `POST /api/v1/diagnostic-tasks/{id}/start`
- `GET /api/v1/pathology-cases/{id}/diagnostic-workbench`
- `POST /api/v1/pathology-reports`
- `POST /api/v1/pathology-reports/{id}/save-draft`
- `POST /api/v1/pathology-reports/{id}/submit`
- `POST /api/v1/pathology-reports/{id}/review`
- `POST /api/v1/pathology-reports/{id}/reject`
- `POST /api/v1/pathology-reports/{id}/sign`
- `POST /api/v1/pathology-reports/{id}/publish`
- `POST /api/v1/report-revision-requests`
- `POST /api/v1/report-revision-requests/{id}/approve`
- `POST /api/v1/report-revision-requests/{id}/reject`
- `POST /api/v1/medical-orders`
- `GET /api/v1/medical-orders/pending`
- `POST /api/v1/medical-orders/{id}/accept`
- `POST /api/v1/medical-orders/{id}/complete`
- `POST /api/v1/medical-orders/{id}/cancel`
- `POST /api/v1/consultations`
- `POST /api/v1/consultations/{id}/participants/{participantId}/comment`
- `POST /api/v1/consultations/{id}/complete`
- `GET /api/v1/pathology-cases/{id}/report-tracking`

## 已验证结果

- 已通过 `DiagnosticWorkflowIntegrationTest`。
- 已通过 `M4RoleAuthorizationIntegrationTest`。
- 已通过 `TechnicalWorkflowIntegrationTest`。
- 已通过 `DiagnosticRevisionIntegrationTest`。
- 已通过 `MedicalOrderIntegrationTest`。
- 已通过 `InternalConsultationIntegrationTest`。
- 已通过 `M4Batch2AuthorizationIntegrationTest`。
- 当前验证结果已覆盖：
  - M4 首批主链路闭环
  - 报告审核退回后重新提交
  - M3 染色完成后自动投递 M4 诊断任务
  - 报告修订审批通过与驳回
  - 技术域医嘱创建、接单、完成、取消
  - 科内会诊发起、参与评论、主持完成
  - 第二批接口越权校验
  - M3 既有主链路回归
- 当前已执行的验证命令为：

```powershell
.\mvnw.cmd -pl bl-center -am clean test '-Dtest=DiagnosticWorkflowIntegrationTest,M4RoleAuthorizationIntegrationTest,TechnicalWorkflowIntegrationTest,DiagnosticRevisionIntegrationTest,MedicalOrderIntegrationTest,InternalConsultationIntegrationTest,M4Batch2AuthorizationIntegrationTest' '-Dsurefire.failIfNoSpecifiedTests=false'
```

## 本批未完成项

- `delay-publish`
- 医生端 `slide_qc_evaluations`
- 外部会诊
- 真实收费集成
- 正式 PDF 渲染与打印
- 更广范围的历史病理跨系统拉取
- 多任务拆分模型下的一病例多诊断任务编排

## 后续建议

- 下一批优先补齐 `delay-publish`、医生端质控录入和外部会诊边界。
- 继续把 M4 服务与 JDBC 仓储按聚合和动作主题细化，降低单服务编排复杂度。
- 补强更广的 M4 回归场景，重点覆盖多病例并行、同病例多次修订、更多版本链和更复杂权限组合。
