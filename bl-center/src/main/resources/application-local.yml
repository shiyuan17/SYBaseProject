spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:blcenter;MODE=LEGACY;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
  flyway:
    enabled: false
  sql:
    init:
      mode: never

security:
  auth:
    jwt:
      allow-generated-keys: true
